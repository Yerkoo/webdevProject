$kind: http-request
name: Logout
method: POST
url: '{{baseUrl}}/api/auth/logout/'
order: 3000
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{accessToken}}'
body:
  type: json
  content: |-
    {
      "refresh": "{{refreshToken}}"
    }
