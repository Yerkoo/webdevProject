$kind: http-request
name: Update Favorite
method: PUT
url: '{{baseUrl}}/api/favorites/1/'
order: 4000
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{accessToken}}'
body:
  type: json
  content: |-
    {
      "course": "{{courseId}}"
    }
